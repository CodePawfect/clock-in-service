spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://admin:admin@localhost:27017/clockin?authSource=admin}

admin:
  initial:
    username: admin
    password: admin

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG
